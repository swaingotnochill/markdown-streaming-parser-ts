{
  "name": "markdown-streaming-parser-example",
  "version": "1.0.0",
  "description": "Example application for markdown-streaming-parser",
  "main": "server/server.js",
  "scripts": {
    "start": "npm run build && node server/server.js",
    "build": "webpack --mode production"
  },
  "dependencies": {
    "markdown-streaming-parser": "file:.."
  },
  "devDependencies": {
    "webpack": "^5.80.0",
    "webpack-cli": "^5.0.2",
    "ts-loader": "^9.4.2"
  }
}
